import 'package:flutter/material.dart';

class SeatTable extends StatelessWidget {
  final width;
  final height;

  SeatTable({Key? key, required this.height, required this.width, required Map<String>})
      : super(key: key);

  @override
  Widget build(BuildContext context) {}
}
